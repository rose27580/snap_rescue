<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= role === 'signup' ? 'Workshop Signup' : 'Workshop Login' %></title>
  <link rel="stylesheet" href="/css/workshop-auth.css">
</head>

<body>
  <div class="container">
    <h1><%= role === 'signup' ? 'Register Your Workshop' : 'Workshop Login' %></h1>

    <% if (error) { %>
      <p style="color:red;"><%= error %></p>
    <% } %>

    <% if (role === 'signup') { %>
      <!-- Signup Form -->
      <form method="POST" action="/workshop/signup">
        <input type="text" name="ownerName" placeholder="Owner Name" required>
        <input type="text" name="workshopName" placeholder="Workshop Name" required>
        <input type="email" name="email" placeholder="Email" required>
        <input type="text" name="phone" placeholder="Phone" required>
        <input type="text" name="location" placeholder="Location" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Sign Up</button>
      </form>
      <p>Already registered? <a href="/workshop/login">Login here</a></p>
    <% } else { %>
      <!-- Login Form -->
      <form method="POST" action="/workshop/login">
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
      </form>
      <p>Don’t have an account? <a href="/workshop/signup">Sign up here</a></p>
    <% } %>
  </div>
</body>
</html>
